kind: pipeline
name: deploy

clone:
  skip_verify: true

steps:
- name: heroku
  image: ruby
  commands:
  - gem install dpl
  - dpl --provider=heroku --app=dragon6 --api-key=$KEY
  environment:
    KEY:
      from_secret: HEROKU_KEY
